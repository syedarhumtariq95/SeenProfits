# Mock data boundary

Future static feature repositories will live here and implement contracts from
`lib/src/domain/repositories`. `MockAdminSessionRepository` is the sole
development-only mock in this phase; it has no API calls or persisted session.
